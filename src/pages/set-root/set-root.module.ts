import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { SetRootPage } from './set-root';

@NgModule({
  declarations: [
    SetRootPage,
  ],
  imports: [
    IonicPageModule.forChild(SetRootPage),
  ],
})
export class SetRootPageModule {}
